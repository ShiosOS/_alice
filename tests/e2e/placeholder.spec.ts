import { describe, expect, it } from 'vitest'

/** Placeholder — full e2e suite lands in task 6. */
describe('e2e placeholder', () => {
  it('keeps the e2e project wired', () => {
    expect(true).toBe(true)
  })
})
